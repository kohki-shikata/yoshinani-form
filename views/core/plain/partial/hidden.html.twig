<div class="form-group">
  <input
      type="{{type}}"
      {{(id ? "id=\"#{id}\"" : null) | raw}}
      {{(name ? "name=\"#{name}\"" : null) | raw}}
      {{(value ? "value=\"#{value}\"" : null) | raw}}
  />
</div>